<Window x:Class="BTactixMotionUI.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BTactixMotionUI"
        xmlns:controls="clr-namespace:BTactixMotionUI.CommonControls"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"  WindowStartupLocation="CenterScreen"
        Width="1000"
        Height="600"
        Title="Shell">

    <Border CornerRadius="16"
            Background="#0F1624"
            BorderThickness="1"
            BorderBrush="#1F2937"
            SnapsToDevicePixels="True">
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <!-- Sidebar -->
                <ColumnDefinition/>
                <ColumnDefinition Width="80"/>
                <!-- Main Content -->
            </Grid.ColumnDefinitions>

            <!-- Sidebar Region -->
            <ContentControl prism:RegionManager.RegionName="SidebarRegion"
                     Grid.Column="0"/>

            <!-- Main Content Region -->
            <ContentControl prism:RegionManager.RegionName="ContentRegion"
                     Grid.Column="1"/>

            <controls:PowerIcon HorizontalAlignment="Center" Grid.Column="2" VerticalAlignment="Top"/>






        </Grid>
    </Border>

</Window>
